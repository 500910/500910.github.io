<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <title>Archet Fine Bows</title>
  <meta name="description" content="Archet Fine Bows — Fine handcrafted bows for discerning musicians.">

  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    html {
      scroll-behavior: smooth;
    }

    body {
      font-family: Georgia, "Times New Roman", serif;
      color: #1d1b18;
      background: #f7f5f0;
      line-height: 1.6;
    }

    a {
      color: inherit;
      text-decoration: none;
    }

    /* =========================
       HEADER
    ========================= */

    header {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      z-index: 10;
      padding: 28px 6%;
      display: flex;
      align-items: center;
      justify-content: space-between;
    }

    .logo {
      display: flex;
      flex-direction: column;
      align-items: center;
      line-height: 1;
      min-width: 150px;
    }

    /*
      Put your logo image in the same GitHub folder as index.html
      and name it:

      logo.png

      If your logo has a different filename, change logo.png below.
    */

    .logo img {
      width: 155px;
      height: auto;
      display: block;
    }

    .nav {
      display: flex;
      gap: 34px;
      align-items: center;
      font-family: Arial, Helvetica, sans-serif;
      font-size: 12px;
      letter-spacing: 2px;
      text-transform: uppercase;
    }

    .nav a {
      position: relative;
      padding-bottom: 5px;
    }

    .nav a::after {
      content: "";
      position: absolute;
      left: 0;
      bottom: 0;
      width: 0;
      height: 1px;
      background: #1d1b18;
      transition: width 0.3s ease;
    }

    .nav a:hover::after {
      width: 100%;
    }

    /* =========================
       HERO
    ========================= */

    .hero {
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      text-align: center;
      padding: 150px 7% 100px;
      position: relative;
      overflow: hidden;
    }

    .hero::before {
      content: "";
      position: absolute;
      width: 700px;
      height: 700px;
      border-radius: 50%;
      border: 1px solid rgba(29, 27, 24, 0.08);
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
    }

    .hero-content {
      position: relative;
      z-index: 2;
      max-width: 900px;
    }

    .eyebrow {
      font-family: Arial, Helvetica, sans-serif;
      font-size: 11px;
      letter-spacing: 5px;
      text-transform: uppercase;
      margin-bottom: 25px;
      opacity: 0.65;
    }

    .hero h1 {
      font-size: clamp(52px, 8vw, 108px);
      font-weight: normal;
      letter-spacing: 4px;
      line-height: 0.95;
      margin-bottom: 30px;
    }

    .hero h1 span {
      display: block;
      font-size: clamp(20px, 2.5vw, 30px);
      letter-spacing: 8px;
      margin-top: 22px;
      font-family: Arial, Helvetica, sans-serif;
      font-weight: normal;
    }

    .hero p {
      max-width: 620px;
      margin: 0 auto 42px;
      font-size: 18px;
      color: #5e5a53;
    }

    .button {
      display: inline-block;
      padding: 15px 34px;
      border: 1px solid #1d1b18;
      font-family: Arial, Helvetica, sans-serif;
      font-size: 11px;
      letter-spacing: 3px;
      text-transform: uppercase;
      transition: all 0.3s ease;
    }

    .button:hover {
      background: #1d1b18;
      color: #f7f5f0;
    }

    /* =========================
       INTRO
    ========================= */

    .intro {
      padding: 120px 8%;
      text-align: center;
      background: #eeece6;
    }

    .section-label {
      font-family: Arial, Helvetica, sans-serif;
      font-size: 10px;
      letter-spacing: 4px;
      text-transform: uppercase;
      margin-bottom: 22px;
      opacity: 0.6;
    }

    .intro h2 {
      font-size: clamp(34px, 5vw, 58px);
      font-weight: normal;
      margin-bottom: 25px;
    }

    .intro p {
      max-width: 720px;
      margin: 0 auto;
      color: #625e57;
      font-size: 17px;
    }

    /* =========================
       COLLECTION
    ========================= */

    .collection {
      padding: 120px 6%;
    }

    .collection-header {
      text-align: center;
      margin-bottom: 65px;
    }

    .collection-header h2 {
      font-size: clamp(36px, 5vw, 58px);
      font-weight: normal;
    }

    .collection-grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 28px;
      max-width: 1200px;
      margin: 0 auto;
    }

    .collection-card {
      min-height: 480px;
      background: #e5e1d8;
      position: relative;
      overflow: hidden;
      display: flex;
      align-items: flex-end;
      padding: 35px;
      transition: transform 0.4s ease;
    }

    .collection-card:hover {
      transform: translateY(-7px);
    }

    .card-1 {
      background:
        linear-gradient(
          145deg,
          rgba(255,255,255,0.25),
          rgba(0,0,0,0.05)
        ),
        #ded9cf;
    }

    .card-2 {
      background:
        linear-gradient(
          145deg,
          rgba(255,255,255,0.2),
          rgba(0,0,0,0.08)
        ),
        #d6d1c6;
    }

    .card-3 {
      background:
        linear-gradient(
          145deg,
          rgba(255,255,255,0.25),
          rgba(0,0,0,0.06)
        ),
        #e2ded5;
    }

    .bow-line {
      position: absolute;
      width: 85%;
      height: 2px;
      background: #2c2924;
      top: 48%;
      left: 7%;
      transform: rotate(-8deg);
      opacity: 0.7;
    }

    .bow-line::before {
      content: "";
      position: absolute;
      left: 0;
      top: -3px;
      width: 8px;
      height: 8px;
      border-radius: 50%;
      background: #2c2924;
    }

    .bow-line::after {
      content: "";
      position: absolute;
      right: 0;
      top: -4px;
      width: 10px;
      height: 10px;
      border-radius: 50%;
      border: 1px solid #2c2924;
    }

    .card-content {
      position: relative;
      z-index: 2;
    }

    .card-content small {
      font-family: Arial, Helvetica, sans-serif;
      font-size: 10px;
      letter-spacing: 3px;
      text-transform: uppercase;
      opacity: 0.6;
    }

    .card-content h3 {
      font-size: 30px;
      font-weight: normal;
      margin-top: 8px;
    }

    /* =========================
       STORY
    ========================= */

    .story {
      display: grid;
      grid-template-columns: 1fr 1fr;
      min-height: 620px;
      background: #24221f;
      color: #f7f5f0;
    }

    .story-image {
      min-height: 550px;
      background:
        linear-gradient(
          rgba(0,0,0,0.18),
          rgba(0,0,0,0.18)
        ),
        #4a4640;
      position: relative;
      overflow: hidden;
    }

    .story-image::before {
      content: "";
      position: absolute;
      width: 75%;
      height: 2px;
      background: rgba(255,255,255,0.7);
      top: 50%;
      left: 12%;
      transform: rotate(-9deg);
    }

    .story-text {
      display: flex;
      flex-direction: column;
      justify-content: center;
      padding: 80px 10%;
    }

    .story-text .section-label {
      opacity: 0.55;
    }

    .story-text h2 {
      font-size: clamp(38px, 5vw, 64px);
      font-weight: normal;
      line-height: 1.05;
      margin-bottom: 28px;
    }

    .story-text p {
      color: #c8c3ba;
      font-size: 16px;
      max-width: 520px;
      margin-bottom: 35px;
    }

    .story-text .button {
      border-color: #f7f5f0;
      align-self: flex-start;
    }

    .story-text .button:hover {
      background: #f7f5f0;
      color: #24221f;
    }

    /* =========================
       CONTACT
    ========================= */

    .contact {
      padding: 120px 7%;
      text-align: center;
    }

    .contact h2 {
      font-size: clamp(38px, 5vw, 62px);
      font-weight: normal;
      margin-bottom: 25px;
    }

    .contact p {
      color: #625e57;
      max-width: 600px;
      margin: 0 auto 35px;
      font-size: 17px;
    }

    /* =========================
       FOOTER
    ========================= */

    footer {
      background: #1d1b18;
      color: #f7f5f0;
      padding: 55px 7% 35px;
    }

    .footer-inner {
      display: flex;
      justify-content: space-between;
      align-items: flex-end;
      gap: 40px;
      max-width: 1200px;
      margin: 0 auto;
    }

    .footer-brand {
      font-size: 25px;
      letter-spacing: 3px;
    }

    .footer-brand span {
      display: block;
      font-family: Arial, Helvetica, sans-serif;
      font-size: 9px;
      letter-spacing: 4px;
      margin-top: 8px;
      opacity: 0.55;
    }

    .footer-right {
      text-align: right;
      font-family: Arial, Helvetica, sans-serif;
      font-size: 10px;
      letter-spacing: 2px;
      opacity: 0.55;
    }

    /* =========================
       MOBILE
    ========================= */

    @media (max-width: 800px) {

      header {
        padding: 22px 6%;
        flex-direction: column;
        gap: 22px;
      }

      .logo img {
        width: 135px;
      }

      .nav {
        gap: 17px;
        font-size: 9px;
        letter-spacing: 1.5px;
      }

      .hero {
        padding-top: 190px;
      }

      .hero::before {
        width: 430px;
        height: 430px;
      }

      .hero p {
        font-size: 16px;
      }

      .collection {
        padding: 85px 6%;
      }

      .collection-grid {
        grid-template-columns: 1fr;
      }

      .collection-card {
        min-height: 390px;
      }

      .story {
        grid-template-columns: 1fr;
      }

      .story-image {
        min-height: 380px;
      }

      .story-text {
        padding: 75px 8%;
      }

      .contact {
        padding: 90px 7%;
      }

      .footer-inner {
        flex-direction: column;
        align-items: flex-start;
      }

      .footer-right {
        text-align: left;
      }
    }
  </style>
</head>

<body>

  <!-- =========================
       HEADER
  ========================== -->

  <header>

    <a href="#home" class="logo">
      <img src="logo.png" alt="Archet Fine Bows">
    </a>

    <nav class="nav">
      <a href="#home">Home</a>
      <a href="#collection">Collection</a>
      <a href="#story">About</a>
      <a href="#contact">Contact</a>
    </nav>

  </header>


  <!-- =========================
       HERO
  ========================== -->

  <main>

    <section class="hero" id="home">

      <div class="hero-content">

        <div class="eyebrow">
          Fine Bows
        </div>

        <h1>
          The Art of
          <span>Fine Bows</span>
        </h1>

        <p>
          Exceptional craftsmanship, refined balance, and timeless
          elegance — created for musicians who demand the finest.
        </p>

        <a href="#collection" class="button">
          Explore the Collection
        </a>

      </div>

    </section>


    <!-- =========================
         INTRO
    ========================== -->

    <section class="intro">

      <div class="section-label">
        Archet Fine Bows
      </div>

      <h2>
        Where Craft Meets Music
      </h2>

      <p>
        A fine bow is more than an instrument accessory.
        It is an extension of the musician — bringing precision,
        sensitivity, character, and expression to every performance.
      </p>

    </section>


    <!-- =========================
         COLLECTION
    ========================== -->

    <section class="collection" id="collection">

      <div class="collection-header">

        <div class="section-label">
          The Collection
        </div>

        <h2>
          Selected Bows
        </h2>

      </div>

      <div class="collection-grid">

        <article class="collection-card card-1">

          <div class="bow-line"></div>

          <div class="card-content">

            <small>
              Violin
            </small>

            <h3>
              Fine Violin Bows
            </h3>

          </div>

        </article>


        <article class="collection-card card-2">

          <div class="bow-line"></div>

          <div class="card-content">

            <small>
              Viola
            </small>

            <h3>
              Fine Viola Bows
            </h3>

          </div>

        </article>


        <article class="collection-card card-3">

          <div class="bow-line"></div>

          <div class="card-content">

            <small>
              Cello
            </small>

            <h3>
              Fine Cello Bows
            </h3>

          </div>

        </article>

      </div>

    </section>


    <!-- =========================
         STORY
    ========================== -->

    <section class="story" id="story">

      <div class="story-image"></div>

      <div class="story-text">

        <div class="section-label">
          Our Philosophy
        </div>

        <h2>
          Made for the Music.
        </h2>

        <p>
          Every bow has its own voice. At Archet Fine Bows,
          we believe in preserving the individuality of each
          instrument while giving the musician the freedom
          to express every nuance.
        </p>

        <a href="#contact" class="button">
          Discover Archet
        </a>

      </div>

    </section>


    <!-- =========================
         CONTACT
    ========================== -->

    <section class="contact" id="contact">

      <div class="section-label">
        Contact
      </div>

      <h2>
        Find Your Bow
      </h2>

      <p>
        For inquiries about available bows, appointments,
        and the Archet collection, please get in touch.
      </p>

      <a href="mailto:info@archetbow.com" class="button">
        Contact Archet
      </a>

    </section>

  </main>


  <!-- =========================
       FOOTER
  ========================== -->

  <footer>

    <div class="footer-inner">

      <div class="footer-brand">
        ARCHET
        <span>
          FINE BOWS
        </span>
      </div>

      <div class="footer-right">
        © 2026 Archet Fine Bows
      </div>

    </div>

  </footer>

</body>
</html>
